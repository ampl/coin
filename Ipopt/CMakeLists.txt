# A CMake build script for the Fortran part of Ipopt.
# It is separate from the main build script to be able to use
# cmake_add_fortran_subdirectory and build Fortran part with a
# MinGW gfortran on Windows while the rest is built with Visual C++.
# This file is published under the Eclipse Public License (EPL).

cmake_minimum_required(VERSION 3.10)

# Remove sh.exe from the path as it breaks MinGW makefile generation.
find_program(SH_PROGRAM sh PATHS ENV PATH NO_DEFAULT_PATH)
if (SH_PROGRAM)
  get_filename_component(path ${SH_PROGRAM} DIRECTORY)
  string(REPLACE "/" "\\" path ${path})
  string(REPLACE "${path}" "" path "$ENV{PATH}")
  set(ENV{PATH} "${path}")
endif ()

project(IPOPTFORT C)

enable_language(Fortran)
include(FortranCInterface)

# Organize output files.
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${IPOPTFORT_BINARY_DIR}/bin)
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${IPOPTFORT_BINARY_DIR}/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${IPOPTFORT_BINARY_DIR}/lib)

# Link the gfortran runtime statically if possible.
if (NOT BUILD_SHARED_LIBS OR CMAKE_GNUtoMS)
  set(CMAKE_SHARED_LINKER_FLAGS
      "${CMAKE_SHARED_LINKER_FLAGS} -static-libgfortran -static-libgcc -static-libstdc++ -static -lwinpthread -lquadmath")
endif ()

if(CMAKE_Fortran_COMPILER MATCHES "gfortran*")
  if(NOT ${CMAKE_Fortran_COMPILER_VERSION} VERSION_LESS 10)
    set (CMAKE_Fortran_FLAGS "-fallow-argument-mismatch  ${CMAKE_Fortran_FLAGS}")
  endif()
endif()

if (CMAKE_GNUtoMS)
  # Under CMAKE_GNUtoMS, libipoptfort.dll (built with MinGW gfortran) needs
  # to expose every BLAS/LAPACK routine that IpBlas.cpp/IpLapack.cpp (built
  # separately, under MSVC, as part of the main "ipopt" library) call - MinGW
  # doesn't export all DLL symbols by default the way ELF .so's do on Linux,
  # so each one has to be force-kept with -u or the MSVC link fails with
  # "unresolved external symbol". This list is every d<x>/id<x> routine
  # IpBlas.cpp/IpLapack.cpp actually reference (double precision only, since
  # this repo never builds Ipopt with IPOPT_SINGLE), plus dsyev_ for Couenne's
  # own sdpcuts code, which calls it directly.
  foreach (sym ddot_ dnrm2_ dasum_ idamax_ dcopy_ daxpy_ dscal_ dgemv_
               dsymv_ dgemm_ dsyrk_ dtrsm_
               dpotrs_ dpotrf_ dsyev_ dgetrf_ dgetrs_ dppsv_)
    set(CMAKE_SHARED_LINKER_FLAGS
        "${CMAKE_SHARED_LINKER_FLAGS} -u ${sym} -u _${sym}")
  endforeach ()
endif ()

# Set F77_FUNC & F77_FUNC_ in cache as they are used in the parent project.
set(F77_FUNC "${FortranCInterface_GLOBAL_MACRO}" CACHE INTERNAL "")
set(F77_FUNC_ "${FortranCInterface_GLOBAL__MACRO}" CACHE INTERNAL "")

# Convert the information about Fortran name mangling into the format
# required by MUMPS.
if (FortranCInterface_GLOBAL_CASE STREQUAL "UPPER")
  add_definitions(-DUPPER)
elseif (FortranCInterface_GLOBAL_SUFFIX STREQUAL "_")
  add_definitions(-DAdd_)
elseif (FortranCInterface_GLOBAL_SUFFIX STREQUAL "__")
  add_definitions(-DAdd__)
endif ()

add_definitions(-DMUMPS_ARITH=MUMPS_ARITH_d)
if (WIN32)
  add_definitions(-DWITHOUT_PTHREAD=1)
endif ()

include_directories(
  Ipopt/src/Common
  ../ThirdParty/Mumps/MUMPS/include
  ../ThirdParty/Mumps/MUMPS/libseq
  ../ThirdParty/Mumps/MUMPS/src
  Ipopt/src/Interfaces)

add_definitions(-DHAVE_CONFIG_H)

# Note: include/mumps_int_def.h (needed by MUMPS 5.x headers) is generated by
# the top-level CMakeLists.txt, not here - configure_file()'s relative output
# path resolves against the current *binary* dir, which for this Fortran
# sub-project is not the same tree the main MSVC "ipopt" target reads its
# includes from, so generating it here wouldn't be visible to that target.

# Prepend directory to each filename passed as varargs and assign the result to var.
function (prepend_dir var dir)
  set(paths )
  foreach (filename ${ARGN})
    set(paths ${paths} ${dir}/${filename})
  endforeach ()
  set(${var} ${paths} PARENT_SCOPE)
endfunction ()

# Source lists below mirror MUMPS 5.9.1's src/Makefile object lists
# (OBJS_COMMON_MOD, OBJS_COMMON_OTHER, OBJS_C_COMMON, OBJS_MOD_ARITHDEP and
# OBJS_OTHER with ARITH=d), built for double precision only. PORD/METIS/SCOTCH
# ordering support is intentionally left out (as in the previous MUMPS 4.10.0
# build here): those are only referenced from code guarded by
# "#if defined(pord)" / "metis" / "scotch", which are never defined below, so
# MUMPS falls back to its always-available built-in AMD/QAMD/AMF orderings.
prepend_dir(mumps_src_common_mod ../ThirdParty/Mumps/MUMPS/src
  ana_blk_m.F ana_omp_m.F ana_orderings_wrappers_m.F double_linked_list.F
  fac_asm_build_sort_index_ELT_m.F fac_asm_build_sort_index_m.F
  fac_descband_data_m.F fac_future_niv2_mod.F fac_maprow_data_m.F
  front_data_mgt_m.F lr_common.F lr_stats.F mumps_comm_buffer_common.F
  mumps_intr_types_common.F mumps_l0_omp_m.F mumps_load.F
  mumps_memory_mod.F mumps_mpitoomp_m.F mumps_ooc_common.F
  mumps_pivnul_mod.F mumps_static_mapping.F omp_tps_common_m.F
  sol_ds_common_m.F sol_omp_common_m.F tools_common_m.F)

prepend_dir(mumps_src_common_other ../ThirdParty/Mumps/MUMPS/src
  ana_blk.F ana_orderings.F ana_set_ordering.F ana_AMDMF.F bcast_errors.F
  estim_flops.F mumps_type2_blocking.F mumps_version.F
  mumps_print_defined.F sol_common.F tools_common.F)

prepend_dir(mumps_src_c_common ../ThirdParty/Mumps/MUMPS/src
  mumps_addr.c mumps_common.c mumps_config_file_C.c mumps_io.c
  mumps_io_basic.c mumps_io_err.c mumps_numa.c mumps_io_thread.c
  mumps_register_thread.c mumps_thread.c mumps_thread_affinity.c
  mumps_flytes.c mumps_sol_omp_memory_manager.c mumps_save_restore_C.c)

prepend_dir(mumps_src_d_mod ../ThirdParty/Mumps/MUMPS/src
  dana_aux.F dana_aux_par.F dana_lr.F dfac_asm_master_ELT_m.F
  dfac_asm_master_m.F dfac_compact_factors_m.F dfac_front_aux.F
  dfac_front_LU_type1.F dfac_front_LU_type2.F dfac_front_LDLT_type1.F
  dfac_front_LDLT_type2.F dfac_front_type2_aux.F dfac_sispointers_m.F
  dfac_lr.F dfac_mem_dynamic.F dfac_omp_m.F dfac_par_m.F
  dfac_sol_l0omp_m.F dlr_core.F dlr_type.F dmumps_comm_buffer.F
  dmumps_config_file.F dmumps_intr_types.F dmumps_lr_data_m.F
  dmumps_mpi3_mod.F dmumps_ooc_buffer.F dmumps_ooc.F dmumps_sol_es.F
  dmumps_save_restore_files.F dmumps_save_restore.F dmumps_struc_def.F
  domp_tps_m.F dsol_lr.F dsol_omp_m.F dstatic_ptr_m.F)

prepend_dir(mumps_src_d_other ../ThirdParty/Mumps/MUMPS/src
  dana_aux_ELT.F dana_dist_m.F dana_driver.F dana_LDLT_preprocess.F
  dana_reordertree.F darrowheads.F dbcast_int.F dend_driver.F
  dfac_asm_ELT.F dfac_asm.F dfac_b.F dfac_diag.F dfac_distrib_distentry.F
  dfac_dist_arrowheads_omp.F dfac_distrib_ELT.F dfac_driver.F
  dfac_lastrtnelind.F dfac_mem_alloc_cb.F dfac_mem_compress_cb.F
  dfac_mem_free_block_cb.F dfac_mem_stack_aux.F dfac_mem_stack.F
  dfac_process_band.F dfac_process_blfac_slave.F
  dfac_process_blocfacto_LDLT.F dfac_process_blocfacto.F
  dfac_process_bf.F dfac_process_end_facto_slave.F
  dfac_process_contrib_type1.F dfac_process_contrib_type2.F
  dfac_process_contrib_type3.F dfac_process_maprow.F
  dfac_process_master2.F dfac_process_message.F dfac_process_root2slave.F
  dfac_process_root2son.F dfac_process_rtnelind.F dfac_root_parallel.F
  dfac_scalings.F dfac_determinant.F dfac_scalings_simScaleAbs.F
  dfac_scalings_simScale_util.F dfac_sol_pool.F dfac_type3_symmetrize.F
  dini_defaults.F dini_driver.F dmumps_driver.F dmumps_f77.F
  dmumps_gpu.c dmumps_iXamax.F dana_mtrans.F dooc_panel_piv.F
  drank_revealing.F dsol_aux.F dsol_bwd_aux.F dsol_bwd.F dsol_c.F
  dsol_distrhs.F dsol_distsol.F dsol_driver.F dsol_fwd_aux.F dsol_fwd.F
  dsol_matvec.F dsol_root_parallel.F dtools.F dtype3_root.F)

set(mumps_src
  ${mumps_src_common_mod} ${mumps_src_common_other} ${mumps_src_c_common}
  ${mumps_src_d_mod} ${mumps_src_d_other}
  ../ThirdParty/Mumps/MUMPS/src/mumps_c.c
  ../ThirdParty/Mumps/MUMPS/libseq/mpi.f
  ../ThirdParty/Mumps/MUMPS/libseq/mpic.c
  ../ThirdParty/Mumps/MUMPS/libseq/elapse.c)

# IpMa28Partition.F (MA28PART) is intentionally not built: it calls the real
# HSL MA28 factorization routine (ma28ad_), which - like all HSL code - isn't
# redistributed here, so nothing provides that symbol. It's only needed by
# Ipopt's optional MA28-based dependency detector (IpMa28TDependencyDetector),
# which is likewise not enabled (HAVE_MA28 is left unset for the "ipopt"
# library below).
add_library(ipoptfort ${mumps_src})

find_package(Threads)
target_link_libraries(ipoptfort lapack blas ${CMAKE_THREAD_LIBS_INIT})

find_library(DL_LIBRARY dl)
if (DL_LIBRARY)
  target_link_libraries(ipoptfort ${DL_LIBRARY})
endif ()

 
if (NOT CMAKE_GNUtoMS)
  #add_ipopt_test(hs071_f SOURCE_DIR ${COIN_BINARY_DIR} hs071_f.f)
endif ()

set(BUILD_SHARED_LIBS False)
add_subdirectory(../ThirdParty/lapack lapack)
